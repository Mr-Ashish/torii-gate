# Hermes Agent config for Torii PR reviews (CI / local).
# Secrets: HERMES_HOME/.env or process env OPENROUTER_API_KEY.

model:
  provider: openrouter
  # Override with TORII_MODEL / OPENROUTER_MODEL (default: Claude Opus 5).
  default: anthropic/claude-opus-5

terminal:
  backend: local

# F41: cap Hermes tool-calling iterations (Hermes default is 500 — too high for CI).
# run-hermes-review.sh rewrites this from vars.TORII_MAX_TURNS (default 40; 0/off disables).
# Complements F36 wall-clock timeout: stops runaway tool loops before the timer.
agent:
  max_turns: 40

# Verbose agent/tool logging → HERMES_HOME/logs/agent.log
display:
  tool_progress: verbose

# Memory lives under HERMES_HOME/memories — preloaded from hub when available.
