# Django/Flask-theme synthetic demo (license-safe)

Original Torii demo code. **Themes only** inspired by common Django/Flask training-app vulnerability classes (SSRF, path traversal, open redirect, SSTI) — **not** a fork of any specific project.

DO NOT deploy. Used for offline labeled eval (`docs/benchmarks/cases/django-vuln-synthetic.json`).
