# F52 / H28 — multi-lens checklist (single-pass)

**Date:** 2026-07-31  
**Status:** shipped (prompt/control-plane; live re-score optional)  
**Tag:** PRODUCT_FEATURE | AGENT_QUALITY

## Problem

After F51 tool-depth lift on odoo#6 (34→39/50), **D10 multi-lens depth** remained
thin: single-pass reviews still collapsed into generic APPROVE + soft nits without
explicit security / tests / performance / API / concurrency coverage. Benchmark and
product research call for multi-dimensional review without multi-agent fan-out cost.

## Fix (minimal)

1. **`agent/review-prompt.md`:** Multi-lens pass (H28) under Review focus + required
   `### Multi-lens checklist` table (correctness, security, tests, performance,
   api_contracts, concurrency, maintainability) with `ok` / `concern` / `n/a`.
2. **`agent/SOUL.md`:** structured-judgment bullet — every `concern` must also be a
   Blocking or Key finding with a trigger scenario.
3. **`scripts/normalize-review.py`:** soft section + heading alias for Multi-lens.
4. **Failure template** + **pack-run-for-ui** expose the section when present.
5. **MEMORY.seed:** always fill Multi-lens checklist.

No second Hermes loop (H8 remains backlog). Single mental pass → structured depth.

## Tests

`tests/test_normalize_review.py::test_multi_lens_section_alias`  
SOUL preflight (F46) clean.

## Next

Live mini re-score on a multi-risk corpus PR (e.g. #2 or #4) to measure D10 lift,
or H25 7th eval PR for evidence diversity.
