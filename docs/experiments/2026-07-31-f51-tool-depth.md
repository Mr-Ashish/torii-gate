# F51 / H26 — tool-depth nudge (changed hunks, not file heads)

**Date:** 2026-07-31  
**Status:** shipped (prompt/control-plane; live re-score = H27)

## Problem

F49 soft re-prompt on odoo eval **#6** recovered tool_turns **0→1**, but the single
tool turn was four parallel `head -80` calls. `odoo/tools/misc.py` header never
includes `street_split` (~L1925), so the agent approved without reading the changed
regex while claiming workspace inspection. Score **34/50** (weakest F49 row; D8=2).

## Fix

1. **F49 re-prompt suffix (H26):** `build_reprompt_suffix` adds **Tool depth (H26 / F51)**
   — forbid `head`-only large-file reads; prefer unified diff hunks and `rg` + line
   range around changed symbols; at least one tool must target a changed region.
2. **Base prompt:** `agent/review-prompt.md` Workspace section — same depth rules for
   first-pass (not only re-prompt).
3. **SOUL Scope:** one bullet so Hermes context load reinforces depth discipline.
4. **Idempotency:** re-prompt block marker is prefix `## Soft re-prompt (Torii H15`.

No new env flag; rides on existing `TORII_TOOL_TURNS_REPROMPT`.

## Tests

`tests/test_tool_turns_gate.py` — `test_build_suffix_tool_depth_h26` + idempotent count.
SOUL preflight still clean under F46 scan.

## Next

**H27:** live mini F49+F51 re-run on Mr-Ashish/odoo#6; compare tool commands and
benchmark D1/D8 vs H24 baseline 34/50.
